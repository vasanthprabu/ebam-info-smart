import React from 'react';

function Logout() {
 const navigate = useNavigate();

  return (
    <div className='reports' style={{textAlign:"center"}}>
      <h1>Logout</h1>
    </div>
  );
}

export default Logout;